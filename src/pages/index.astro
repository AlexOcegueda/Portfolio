---
import MainLayout from '../layouts/MainLayout.astro';
import projectData from '../components/projects.json';
import bookData from '../components/books.json';
import movieData from '../components/movies.json';

const projects = projectData;
const movies = movieData;
const books = bookData;
---

<MainLayout>
  <div class="circle-container">
    <div class="circle" id="circle"></div>
    <div class="dot" id="dot"></div>
  </div>

  <section id="intro" class="section-container">
    <div class="background-gif"></div>
    <div class="overlay"></div>
    <div class="intro-description">
      <h1 id="name-tag" >Alex Ocegueda Castro</h1>
      <p>
       Hello! I’m Alex Ocegueda <span class="ipa">/oh-seh-geh-dah/</span>, —a software engineer with experience in full-stack development.<br /> <br />
       </p>
      <p>Reach out: <a href="mailto:alexocegueda59@gmail.com" rel="noopener noreferrer" target="_blank">[alexocegueda59@gmail.com]</a></p>
      <p>Socials: <a href="https://github.com/AlexOcegueda" rel="noopener noreferrer" target="_blank">[Github]</a> 
      <a href="https://www.linkedin.com/in/alexocegueda/" rel="noopener noreferrer" target="_blank">[LinkedIn]</a>
      <a href="/resume.pdf" rel="noopener noreferrer" target="_blank">[Resume]</a></p>
    </div>
  </section>

  <section id="interests-container" class="section-container">
    <div class="heading-container" id="interests">
      <h2>Interests</h2>
    </div>
    <!-- <div class="interest-section">
      <h3>Languages</h3>
      <p>I like learning new languages for fun. I have a 720-day streak on Duolingo - most being spent on Spanish but
      I recently decided to learn German and Italian. I also practice American Sign Language (ASL) from a website called
      <a href="https://www.lifeprint.com/asl101/lessons/lessons.htm">[Lifeprint]</a>. I finished ASL 1 on there and plan to do up to course 4.</p>
    </div> -->
    <div class="interest-section">
      <h3>Interests</h3>
      <p>Beyond coding, I enjoy exploring stories through books and movies. Here are some of my favorites:</p>
      
      <div class="grid-container">
        <!-- Books Section -->
        <div class="grid-column">
          <h3>📖 Favorite Books</h3>
          {books.map((book, index) => (
            <div class="grid-item" key={index}>
              <img src={book.img} alt={book.name} />
              <div class="title-author">
                <h4>[<a href={book.link} target="_blank" rel="noopener noreferrer">{book.name}</a>]</h4>
                <p>By: {book.author}</p>
              </div>
            </div>
          ))}
        </div>

        <!-- Movies Section -->
        <div class="grid-column">
          <h3>🎬 Favorite Movies</h3>
          {movies.map((movie, index) => (
            <div class="grid-item" key={index}>
              <img src={movie.img} alt={movie.name} />
              <div class="title-author">
                <h4>[<a href={movie.imdb} target="_blank" rel="noopener noreferrer">{movie.name}</a>]</h4>
                <p>Directed by: {movie.author}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>

  </section>

  <div class="heading-container" id="projects">
    <h2>Projects</h2>
  </div>
  <main class="projects">
    {projects.map((project, index) => (
      <div class="project-container fade-in-out" data-project-id={project.id}>
        <div class="text-container">
          <h3>
            {project.displayName}
            {project.website && (
              <a href={project.website} target="_blank" rel="noopener noreferrer"> [Website]</a>
            )}
          </h3>
          <span id="tool">{project.tools.join(', ')}</span>
          <p class="project-description">{project.description}</p>
          <a class="github-link" href={project.link}>See on Github →</a>
        </div>
        <img src={project.image} alt="Project Image" style="border-radius: 1rem; object-fit: cover; max-width: 100%; height: auto;" />
      </div>
    ))}
  </main>
  <audio id="audio-clip" src="/audio/ocegueda.mp3" style="display: none;"></audio>

   <script>
    document.addEventListener("DOMContentLoaded", function() {
      const ipaSpan = document.querySelector('.ipa');
      const audioClip = document.getElementById('audio-clip');

      ipaSpan.addEventListener('click', function() {
        audioClip.play();
      });
    });
  </script>

  <script type="module" src="/scripts/animations.js"></script>
</MainLayout>
